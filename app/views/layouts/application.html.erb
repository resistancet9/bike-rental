<!DOCTYPE html>
<html>

<head>
  <title>RentABike</title>
  <%= csrf_meta_tags %>
    <%= stylesheet_link_tag    "application", media: "all", "data-turbolinks-track" => true %>
      <%= stylesheet_link_tag    'https://stackpath.bootstrapcdn.com/bootstrap/4.1.3/css/bootstrap.min.css' %>
        <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
</head>

<body>
  <div class="fixed-top d-flex flex-column flex-md-row align-items-center p-3 px-md-4 mb-3 bg-white border-bottom shadow-sm">
    <h3 class="my-0 mr-md-auto font-weight-bold">Rent A Bike</h3>
    <nav class="my-2 my-md-0 mr-md-3">
      <%= link_to("Home", home_path, {:class => "p-2 text-dark"}) %>
        <%= link_to("Contact", contact_path, {:class => "p-2 text-dark"}) %>
          <%= link_to("About", about_path, {:class => "p-2 text-dark"}) %>
            <% if signed_in? %>
              Signed in as:
              <%= current_user.email %>
                <%= link_to 'Sign out', sign_out_path, method: :delete, :class => "p-2 text-dark" %>
                  <% else %>
                    <%= link_to 'Sign in', sign_in_path, :class => "p-2 text-dark"%>
                      <%= link_to 'Sign up', sign_up_path, :class => "p-2 text-dark"%>
                        <% end %>

    </nav>
  </div>
  <div class="adjustment"></div>
  <%= yield %>
    </div>
</body>

</html>